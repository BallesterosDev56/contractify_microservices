services:
  - type: web
    name: contract-service
    runtime: python
    buildCommand: bash build.sh
    startCommand: bash start.sh
    envVars:
      - key: DATABASE_URL
        scope: build
        sync: false
      - key: PYTHONUNBUFFERED
        value: "1"
